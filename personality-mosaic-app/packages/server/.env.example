# Server Configuration
PORT=3001
NODE_ENV=development # Options: development, production, test

# Database (MongoDB)
MONGODB_URI=mongodb://localhost:27017/personalityMosaicDB
# For production, use a robust connection string:
# MONGODB_URI_PROD=mongodb+srv://<username>:<password>@<cluster-url>/personalityMosaicDB?retryWrites=true&w=majority

# JWT Secrets
JWT_SECRET=aVeryStrongAndLongRandomSecretForJWTSigning
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_SECRET=anotherVeryStrongAndLongRandomSecretForRefreshTokens
REFRESH_TOKEN_EXPIRES_IN=7d # Or "30d"

# CORS Configuration (Example: allow client origin)
CLIENT_ORIGIN=http://localhost:3000

# Social Login Credentials (Placeholders - keep sensitive values out of version control)
# GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
# GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET
# GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# Email Service (Example for a transactional email provider)
# EMAIL_PROVIDER_API_KEY=
# EMAIL_SENDER_ADDRESS="noreply@yourdomain.com"

# Logging Level (Example: info, warn, error, debug)
# LOG_LEVEL=info
